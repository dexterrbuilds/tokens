FROM node:22-alpine
WORKDIR /app
COPY apps/cloudrun-db-canary/package.json ./package.json
RUN npm install --omit=dev --ignore-scripts
COPY apps/cloudrun-db-canary/src ./src
USER node
EXPOSE 8080
CMD ["node", "src/index.mjs"]
